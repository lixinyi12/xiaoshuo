
添加多人协作写作功能
用户头像上传
server-routes-user部分可以分割
把所有alert改成自定义的弹窗
将阅读页面的各个部分拆分为多个组件
将发布页面的各个部分拆分为多个组件
后端传的数据改为纯js格式，前端再做样式优化
实现分页查询
添加个人主页查看被点赞记录功能
将相似的函数（getUserById、getChaptersById、getNovelById）统一为getById
类型标准化
接入ai，ai审核->人工审核（diffy）
完善作者认证审核：
- 安全性：后台接口必须验证管理员权限，防止普通用户通过接口直接修改审核状态。
- 防刷：对用户提交申请做频率限制（如一天只能提交一次）。
- 重新申请：如果申请被拒绝，用户可以修改信息后再次提交。可以在前端引导用户重新填写，后端需将旧记录标记为无效（或新建一条记录），同时保留历史记录以便追溯。
- 审核记录：保留所有审核记录，包括操作人、时间，方便日后审计。